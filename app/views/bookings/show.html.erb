<% content_for :title, @booking.pnr %>
<%= link_to "← my bookings", bookings_path, class: "back" %>

<h1><%= @booking.pnr %></h1>
<p class="meta"><%= @booking.state %> · <%= rupees(@booking.total) %></p>
<p>
  <%= @booking.trip.origin %> → <%= @booking.trip.destination %>
  · <%= @booking.trip.depart_at.strftime("%d %b %Y, %H:%M") %>
  · <%= @booking.trip.bus.operator.name %>
</p>
<ul>
  <% @trip_seats.each do |trip_seat| %>
    <li><%= trip_seat.seat.number %> · <%= rupees(trip_seat.fare) %></li>
  <% end %>
</ul>

<% if @booking.cancelled? %>
  <p>refund <%= rupees(@booking.refund) %></p>
<% end %>

<% if @booking.can_cancel? %>
  <div class="actions">
    <%= button_to "cancel (Rs 50 fee)", cancel_booking_path(@booking.pnr), method: :post, data: { turbo_confirm: "Cancel this booking? Refund is fare minus Rs 50." } %>
    <%= link_to "reschedule to another trip", reschedule_booking_path(@booking.pnr) %>
  </div>
<% end %>
